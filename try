const Toast = ({ content, onConfirm, onCancel }) => {
  return (
    <div className="toast">
      <p>{content}</p>
      <div className="toast-buttons">
        <button onClick={onConfirm}>OK</button>
        <button onClick={onCancel}>Cancel</button>
      </div>
    </div>
  );
};
